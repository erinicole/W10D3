import React from 'react';
import Tile from "./tile"
 
const BoardView = (props) =>{
  debugger
  return (
  <div>{props.board.grid.map(row,rowidx)
    props.board.grid.map(tile,colidx){
      return 
    }
  
  }
  </div>
  )


export default BoardView;